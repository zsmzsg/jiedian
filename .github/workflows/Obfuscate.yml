name: Build Obfuscate BPB Panel

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 1 * * *"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: 安装依赖
        run: |
          npm install -g javascript-obfuscator
          sudo apt-get update

      - name: 下载最新的 BPB Worker 文件
        run: |
          wget https://github.com/bia-pain-bache/BPB-Worker-Panel/releases/latest/download/unobfuscated-worker.js -O origin.js
          if [ ! -f "origin.js" ]; then
            echo "错误：未下载到 unobfuscated-worker.js"
            # 列出最新版本的资产以便调试
            curl -s https://api.github.com/repos/bia-pain-bache/BPB-Worker-Panel/releases/latest | jq '.assets[] | .name'
            exit 1
          fi

      - name: 混淆 BPB Worker JS
        run: |
          javascript-obfuscator origin.js --output _worker.js \
            --compact true \
            --identifier-names-generator hexadecimal \
            --rename-globals true \
            --string-array true \
            --string-array-encoding 'base64' \
            --string-array-threshold 0.75 \
            --transform-object-keys true \
            --self-defending false \
            --simplify true
          if [ ! -f "_worker.js" ]; then
            echo "错误：未生成混淆后的 _worker.js"
            exit 1
          fi

      - name: 调试文件状态
        run: |
          ls -la
          cat _worker.js | head -n 10

      - name: 提交并推送更改
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: ':arrow_up: 更新最新的 BPB Panel'
          commit_author: 'github-actions[bot] <github-actions[bot]@users.noreply.github.com>'
          file_pattern: '_worker.js'
